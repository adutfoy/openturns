Create and Sample a Normal distribution
=======================================

Create a Normal distribution with mean :math:`\mu=0` and standard deviation :math:`\sigma=1`.

Get a realization and an iid sample of size :math:`N`.

.. literalinclude:: t_NormalDist.py
